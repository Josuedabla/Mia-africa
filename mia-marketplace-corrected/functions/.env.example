# These are set as Cloud Functions SECRETS (not a committed .env), via:
#   firebase functions:secrets:set GEMINI_API_KEY
#   firebase functions:secrets:set ALGOLIA_WRITE_KEY
#   firebase functions:secrets:set CHARIOW_API_KEY
# This file only documents which secrets exist and their non-secret
# companion config. Never put real key values in this file.

# Non-secret, safe to keep as plain functions config / env:
ALGOLIA_APP_ID=LUVI2Y21SH
ALGOLIA_INDEX_NAME=mia_products
CHARIOW_API_URL=https://api.chariow.com/v1

# Secrets (set via `firebase functions:secrets:set`, never written here):
# GEMINI_API_KEY
# ALGOLIA_WRITE_KEY
# CHARIOW_API_KEY
